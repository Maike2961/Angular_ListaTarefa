services:
  mongodb:
    container_name: mongo_container
    image: mongo:latest
    environment:
        MONGO_INITDB_ROOT_USERNAME: root
        MONGO_INITDB_ROOT_PASSWORD: root
    ports:
      - "27015:27017"
    volumes:
      - mongo_data:/var/data

volumes:
  mongo_data: